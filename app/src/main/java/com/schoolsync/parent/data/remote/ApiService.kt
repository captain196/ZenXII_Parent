package com.schoolsync.parent.data.remote

/**
 * Retrofit API service interface.
 * Auth endpoints removed — authentication is now handled directly
 * by Firebase Auth SDK (signInWithEmailAndPassword).
 * This interface is kept for any future non-auth REST endpoints.
 */
interface ApiService
